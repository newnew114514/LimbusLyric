H95F10F4 BILINGUAL PRESET + RELEASE COVER (2026-09-07)
- Restores bilingual secondary-lane presentation parity: translation text resolves its own script-aware H51 font/size preset, user entrance motion, living shake, and mature selected exit effect while keeping H95F10F3 cached-sprite performance.
- Translation fragment exits own their own character order and never map onto primary glyph indices; bounded prewarm now primes the translation-resolved font.
- Makes cover identity release-aware with a cover-only, strict-session GSMTC album probe, release-salted artwork cache, and ambiguous same-track/multi-release rejection instead of taking the first artwork; protected media polling is unchanged.
- Preserves player clock/seek/provider/primary-renderer authority and the existing H95F2 cache-migration contract. Canonical release suite adds H95F10F4 replay coverage.

H83 CODEX SAFE SALVAGE (2026-09-06)
- Ports the useful NetEase same-title/different-track-ID lyric recovery from the older Codex experimental branch onto H82 without importing the old branch wholesale.
- Native NetEase track ID constrains lyric cache/search/result identity only; it never gains clock/seek/playback authority.
- NetEase cache rows are ID-scoped when a trusted native ID exists; wrong-ID and late stale results are rejected before commit.
- Stable same-title ID conflicts reuse the mature H38 refetch path with bounded retry; NetEase native startup/stop cleanup and same-text row-material style ownership also recover the low-risk Codex H73 fixes. Shared `_poll_loop` redesign remains intentionally deferred. Canonical release suite: 146 gates.

H81 LYRIC IDENTITY FIREWALL (2026-09-05)
- Prevents unknown-duration or fast-stage hidden lyric candidates from becoming selected-player transport duration; words may display while duration stays player-owned.
- QQ pre-bind track-switch hints no longer short-circuit post-bind duration proof; validated current-player duration triggers version-authorized refetch on conflicts.
- Separates lyric-provider hints from playback transport: foreign NetEase Bridge identity/duration and QQ duration resolution cannot leak into another selected player.
- Adds safe QQ/KuGou cache hygiene for unknown-duration and key/payload-duration-conflicting rows.
- Leaves KuGou/NetEase core clock+seek, QQ source-locked clock/seek bodies, H80 UI, dependencies and one-click packaging commands unchanged. Canonical release suite: 143 gates.

H80 STABLE STUDIO STAGE + LANDMARK MICRO MOTION (2026-09-05)
- Retires H79 whole-page snapshot rise instead of tuning its duration/FPS again; page content now switches directly and only semantic landmarks receive micro-motion.
- Re-homes the existing H74 subtitle-style and exit-motion preview cards into one fixed Studio Stage above the scrolling inspector, preserving the original widgets/signals/config owners.
- Keeps H78 navigation continuity, then animates only the workspace title (145ms/5px), first visible section title (125ms/4px), and the small Studio Stage mode switch (120ms). Parameter rows never stagger/fly/scale.
- H79 live lyric/ambient Hero, H76 blur lifecycle and all player/provider/seek/config ownership remain unchanged. Canonical release suite: 142 gates.

H79 LIVE LYRIC SHELL + SMOOTH SNAPSHOT TRANSITION (2026-09-05)
- Renames the creative Appearance destination back to 字幕 / LYRICS so it cannot be confused with application-shell appearance.
- Replaces H78's dense-page QGraphicsOpacityEffect with one captured settings-region surface that settles upward 10px over 165ms and is immediately destroyed; the expensive widget tree itself is not opacity-animated.
- Makes control-panel micro-animation cadence screen-refresh-aware (60–120Hz while active) and upgrades H74 Motion Preview from fixed 24ms/~41.7fps to the same bounded refresh-aware interval.
- Adds an actual current-lyric sample to Now Playing using the existing LyricWindow text/font/color, plus a restrained ambient tint from existing cover-follow/current lyric color state; no new artwork fetch or worker.
- No player/provider/seek/desktop-exit/config ownership changes. Canonical release suite: 141 gates.

H78 STUDIO NAVIGATION + TYPOGRAPHIC LAYOUT (2026-09-05)
- Keeps the title utility permanently named 界面, re-homes the mature H51 per-language fonts/per-line size range/center-region/multiscreen card into 外观, and preserves original control/config owners.
- Strengthens the prior optically-weak page transition and adds a moving studio navigation indicator plus editorial workspace index.
- No player/provider/seek/desktop-exit/config ownership changes. Canonical release suite: 140 gates.

H77 APPLICATION SHELL + INSPECTOR SYSTEM (2026-09-05)
- Centralizes control-panel scale + custom background in the title-bar “界面” utility; duplicate Settings-page application-interface/background cards remain hidden compatibility owners and are excluded from global search.
- Reweights the existing Hero into a Now Playing surface: current track context is primary, app branding/status become quiet supporting layers, with no new media polling.
- Introduces an in-place Inspector Row grammar for existing settings: normalized spacing, semantic labels/values, hairline row separation and lower-contrast combo/spin/edit/slider material without replacing mature controls.
- Adds quiet workspace kickers while preserving all five stable tab indices and H76 motion tokens. No player/provider/seek/desktop-exit or config ownership changes. Canonical release suite: 139 gates.

H76 EDITORIAL POLISH + WIPE REFINEMENT + BLUR RECOVERY (2026-09-05)
- Hides the legacy product-facing 退场质感 compatibility row; stored renderer mode remains unchanged for old configs/presets.
- Shows the target-display selector only when more than one monitor is detected and reacts to screen add/remove.
- Replaces the ambiguous icon-only background shortcut with a compact palette + 背景 utility, reduces card/border density, and adds a restrained motion grammar: 165ms page settle plus a 180ms background flyout rise/fade.
- Keeps H71 per-character exit unchanged; directional wipes now use one faint short veil instead of the H71 double distant ghost trail.
- Field-log closure: tail/orphan blur history can no longer reach ~0.94 and disappear via H62 age-blur-invisible before release. It is promoted through the existing begin_fade/fading lane, then receives the visible H76 release tail. Transient blur errors also hold/retry instead of immediate retirement.
- H75 replay is scope-capped at the H76 marker only; H75 runtime behavior is unchanged. Canonical release suite: 138 gates.

H75 INFORMATION ARCHITECTURE + SPOTIFY PARITY (2026-09-05)
- Makes Motion visual-only and re-homes player display offsets plus Overlay/OBS/capture mode into Settings → 同步与输出 through compatibility proxies.
- Global settings search now spans all five pages, preserves the query across navigation and jumps to a page with a match instead of silently clearing.
- Spotify gains its own persisted display offset and first-class per-song DIY rule slot; schema-v5 normalization preserves Spotify while ALL/per-player switching now covers four players.
- Retires the product-facing H12 mixed Display+Update card: UI scale and updater get semantic surfaces, raw HTTPS source moves behind an overflow action, while mature H12 controls/config keys remain owners.
- No playback/provider/seek/desktop-exit authority or new runtime dependency is introduced. Canonical release suite: 137 gates.

H74 PREVIEW STAGE + APPEARANCE INSPECTOR (2026-09-05)
- Adds a persistent vector LIVE STYLE stage to Appearance and an exit-only replay stage to Motion; both are control-panel previews and never own player/lyric timing.
- H12 continuous font weight, lyric opacity and cover-follow color move back into the Appearance workflow through proxy controls while the original H12 widgets/config keys remain authoritative.
- Appearance order becomes 基础表现 / 样式预设 / 字体与轮廓 / 光与材质; Motion becomes 退场 / 入场 / 运动 / 空间方向 / 同步 beneath its preview stage.
- Preset CRUD is collapsed into one outline overflow menu using the existing preset methods. Motion preview is capped to 18 characters and ~42fps (24ms) to remain a bounded UI affordance.
- No player/provider/seek/desktop-exit ownership, new runtime dependency, SVG resource, network call or worker thread is added. Canonical release suite: 136 gates.

H73 EDITORIAL INSTRUMENT UI (2026-09-05)
- Refines the control panel into a quieter editorial/instrument shell without changing playback, provider, seek, lyric timing or overlay renderer ownership.
- Title bar now uses restrained outline controls and a discoverable photo-edit background utility; the utility opens a compact panel for background enable/image/strength/clear while the Settings-page controls remain available.
- Product navigation becomes 播放 / 外观 / 动效 / 单曲 / 设置. Underlying tab indices remain unchanged for config compatibility; only the visual order places 单曲 before 设置.
- The old 中文/英文 “模式” is correctly presented as 退场质感：完整字形 / 轮廓余韵 while preserving legacy stored data keys. Hxx generation prefixes are removed from product-facing section headings.
- Runtime icons are dependency-free QPainter primitives following the MIT Tabler Icons outline language; no SVG/font/plugin packaging dependency is added. Canonical release suite: 135 gates.

H72 EXIT LIFECYCLE + DIRTY-REGION CLOSURE (2026-09-05)
- Closes H71 flavor ghost-pixel risk by expanding sparse dirty accounting to every effect-native motion envelope and explicitly invalidating the final envelope on both natural and direct-drop retirement.
- H68 active blur deadlines and integration now follow sampled playback position, so pause time cannot consume future lyric budget or blur progress.
- Tail history rows with no possible future capacity eviction return to playback-clock H62 natural retirement instead of parking faintly at H68's 0.939 active ceiling.
- History-to-fading blur handoff preserves the already-visible optical floor; a 15s watchdog exists only as pathological terminal insurance and does not change normal H70 speed semantics.
- Provider/player/seek authority, H71 artistic amplitudes, Atlas rasterization and packaging commands are unchanged. Canonical release suite: 134 gates.

H71 FLAVOR PRESENCE + INSTANT UI CLEANUP (2026-09-05)
- Recalibrates H70 optional flavor amplitude after field feedback that several 0→100% sliders were too visually conservative at normal desktop scale. 0% still delegates the exact H70/H69 path.
- Mid-slider is now intentionally readable: per-character scatter gains larger stable drift/rotation, directional wipes gain stronger two-stage glyph trails, shrink/drift/hop/depth flavors gain materially larger bounded motion. Classic fade rise is unchanged.
- Per-character/wipe flavor rows request the existing H61 async row-material worker when needed, reducing cases where fragment-material fallback silently erased the optional flavor. No synchronous raster work is added to paintEvent.
- Selecting direct disappear now hides both 退场速度 and 专属风味 rows completely; switching back restores the remembered per-effect controls.
- H70 speed preference, dense-song soft deadline adaptation, H68/H69 blur timing/monotonic continuity, player/seek/provider authority and packaging semantics are unchanged. Canonical release suite: 133 gates.

H70 PER-EFFECT EXIT PROFILES + OPTIONAL FLAVOR (2026-09-05)
- Every exit effect now remembers its own user-facing 退场速度 plus one effect-native optional flavor; flavor=0 delegates the mature pre-H70 renderer.
- Flavor mapping: whole-row fade=上升距离, per-character=离散度, directional wipe=拖尾, quick shrink=蓄力, soft drift=重力感, hop/drop=弹性, progressive blur=景深扩散; direct disappear stays parameter-free.
- Speed remains a user preference under adaptation: ordinary sections follow it directly, while future-cadence budgeting only soft-compresses impractical tails in dense bursts and never modifies flavor.
- H68/H69 progressive blur keeps its deadline/monotonic/velocity-continuity engine; H70 only adds per-effect speed intent and an optional depth-spread presentation layer.
- Old fade/rise settings and old full-style presets migrate deterministically. New presets/config remember the full per-effect profile map. Canonical release suite: 132 gates.

H68 DEADLINE BLUR PLANNER (2026-09-04)
- Replaces H67's fixed pre-eviction 0.875 target with a whole-lifecycle deadline plan using known future lyric timestamps plus a bounded visible release tail.
- Ultra-dense 117-150ms rows are allowed to overflow capacity partway through the blur and complete the remaining motion after eviction, instead of requiring an impossible last-moment speed spike.
- Active-to-release handoff preserves progress velocity with a monotone cubic Hermite tail; render-pressure/queue adjustments are snapshotted at release start instead of retargeting the tail every frame.
- Blur-decay rows are protected from normal-play burst direct-clear; their placement collision weight fades with optical readability so high-density admission can recover without popping the animation.
- Player timing, seek/provider authority, lyric acquisition, Atlas rasterization, packaging commands and non-blur exit semantics are unchanged. Canonical release suite: 131 gates.

H65 CONTINUOUS BLUR LIFECYCLE (2026-09-04)
- Fixes H64 active-history blur progress jumps/reversals by integrating fill-dependent speed over elapsed time instead of multiplying total age by the current fill speed.
- Provider-idle/instrumental and capacity=1 transitions rebase H62's raw hold clock to the exact visible progress, preventing parked rows from jumping straight to invisible.
- Max cached defocus now completes before opacity loss; the final tail holds maximum blur while fading, with smoother stage blending and no new paint-time raster work.
- Player timing/seek/provider authority and H63/H62 release ownership are unchanged. Canonical release suite: 127 gates.

H52 KUGOU ZERO CROSS-PROCESS ACCESSIBILITY (2026-09-03)
- All seven supplied affected-user logs are packaged Windows build 26200; the latest H48 session proves the wake was gone but a HostV2 Range call still returned after shutdown began, leaving the native worker alive until the 5.2s hard-exit watchdog.
- Build 26200 now reuses the complete H22/H23 KuGou fail-closed path: no HostV2 UIA Range, point/hidden UIA, UIA initialization, MSAA clock, or MSAA version-duration traversal. Existing Win32, GSMTC, KRC, numeric-memory, visual-rail and gesture paths remain available.
- Scope is exact: existing legacy Win10 policy remains; build 26100, source/debug mode, non-Windows and other players keep their prior behavior.
- Canonical release suite: 114 gates.

H48 KUGOU NONINVASIVE ACCESSIBILITY + WARMUP GLOW (2026-09-03)
- Packaged Windows KuGou no longer receives the generic WM_GETOBJECT/pywinauto accessibility wake that the build-26200 field log showed touching KuGou's Chromium tree as playback began.
- Existing shallow HostV2 Range, GSMTC, Win32 geometry, visual rail, track identity, duration and seek authority rules are unchanged; QQ/NetEase and source-mode behavior are unchanged.
- Ordinary background Atlas warmup keeps the normal glow-capable renderer. The existing measured 120ms hard-paint fuse still falls back to plain glyphs if rendering is genuinely unsafe.
- Canonical release suite: 110 gates.

H44 RUNTIME LATENCY + VISUAL QUALITY RECOVERY (2026-09-01)
- KuGou ordinary newly-started tracks on frozen Windows receive an immediate display-only monotonic clock instead of waiting tens of seconds for HostV2 proof; startup late-attach synthetic zero remains forbidden.
- The first positive identity-bound HostV2 Slider sample (>=120ms) may calibrate display-only position; formal duration/seek authority remains on the existing proof chain.
- NetEase native identity can no longer combine a new player duration with an old loaded payload to launch repeated H38 version refetch; stale async results are rejected, conflicting cache is evicted, and failed same-identity refetch keeps debounce.
- A worker-only whole-song Atlas build-budget fuse stops Atlas precompute but no longer permanently drops glow/vector rendering or caps visual cadence; genuine GUI paint-hard-budget still fails closed to the emergency renderer.
- Canonical release suite: 106 gates.

H43 KUGOU CLOCK DEADLOCK + STARTUP MOTION SAFETY (2026-09-01)
- Fixes packaged KuGou PLAYING/raw=0 startup deadlock: instrumental cards get presentation-only monotonic time.
- First non-zero plausible HostV2 Slider sample may seed display-only position before formal multi-sample proof.
- Zero Host samples remain quarantined; duration/seek authority rules are unchanged.
- Packaged Windows skips StartupMotion because affected 26200 logs repeatedly faulted at StartupMotion.show_centered/QWidget.show.

H42P2 RELEASE RESUME HOTFIX (2026-09-01)
- Runtime is unchanged from H42/H42P1.
- Adds RESUME_AFTER_H29_GATE_FIX.cmd for the specific 103/104 H29 gate-scope failure.
- Resume reuses the existing build venv/dependencies, rechecks metadata + H29 + H42 + runtime profile, then continues at packaging.
- Fresh/normal releases still use BUILD_END_USER_INSTALLER.cmd and the full 104-gate suite.

H42P1 RELEASE GATE SCOPE FIX (2026-09-01)
- Runtime behavior is byte-for-byte unchanged from H42; main-source lock remains identical.
- Fixes CHECK_NETEASE_TEMPORAL_RAIL_CACHED_WITNESS_H29_REPLAY.py incorrectly scanning every later H30-H42 layer for the token `EnumProcesses`.
- H29 replay now scopes its no-EnumProcesses assertion to the H29 layer only (`# H29` through `# H30`), preserving the original CloudMusic cached-PID contract while allowing H42's independent bounded PSAPI helper.
- Canonical release suite remains 104 gates.

H42 FROZEN KUGOU CAPABILITY SAFETY (2026-09-01)
- Fixes packaged Windows 26200 users who were actually running H41 but missed H37-H41 KuGou safety because those guards were limited to build <22000.
- Same-track HostV2 0ms can no longer overwrite a progressed absolute rail clock without independent user/physical near-zero restart proof; false zero cannot advance transport generation.
- Proven HostV2 duration remains usable as player-owned duration on capable Win11 builds; H42 does not disable the useful Range lane.
- Process cyclic-GC protection now covers all frozen Windows before Qt startup; QQ/KuGou PID probes use PSAPI instead of tasklist/subprocess, including reader workers.
- H41 fake-town-baby lyric completeness protection and QQ/NetEase fixes remain unchanged.
- Canonical release suite: 104 gates.

H39 WIN10 KUGOU AUTHORIZED SEEK + COM LIFETIME (2026-08-31)
- Win10 frozen KuGou rail clicks/drags now require H38 player-owned duration at the final target/commit boundary; stale lyric/KRC duration can no longer bypass `seek-scale=disabled`.
- If player duration is still unknown, the real KuGou click is left untouched but LimbusLyric publishes no guessed seek; it waits for GSMTC or H38 scale-free physical rail-rate evidence.
- Win10 frozen disables automatic cyclic GC before the Qt event loop, closing the startup window that still produced `Garbage-collecting -> comtypes Release -> nativeEvent` 0x8001010d traces. Reference counting remains active; Win11 is unchanged.
- NetEase accepted payload duration/key refreshes H38 reconciliation metadata so an already-correct version is not repeatedly refetched.
- Canonical release suite: 101 gates.

H38 CROSS-PLAYER VERSION AUTHORITY + WIN10 SAFE SEEK (2026-08-31)
- Lyric/KRC duration is presentation metadata until independently corroborated by player-side evidence on Win10 KuGou.
- Cross-player same-title tracks are version-provisional; player-duration conflicts trigger duration-anchored lyric re-selection.
- Win10 KuGou can infer player duration from scale-free physical rail motion without re-enabling unsafe deep UIA/MSAA.
- Recent explicit KuGou seeks veto ambiguous same-identity GSMTC zero/restart noise.
- Canonical release suite: 100 gates.

H37P1 RELEASE GATE PORTABILITY (2026-08-31)
- Runtime behavior is unchanged from H37; this patch fixes a Windows/VM-only flaky H22 replay gate.
- H22 slow-provider replay no longer depends on sleep(12ms) crossing a 5ms wall-clock threshold.
- The replay now enters the slow-fuse branch deterministically while production H22 keeps its 1500ms threshold and H33F1 mandatory next-call skip.
- Canonical release suite remains 99 gates.

H37 RUNTIME RECONCILIATION + UI RECOVERY (2026-08-31)
- Win11 emergency history keeps shake/scale motion; QQ minimized-window stale rewind veto.
- Win10 KuGou stable seek geometry + narrow mouse rail; negative liveness backoff.
- Restored balanced boxed titlebar controls.
- Canonical release suite: 99 gates.

H35 CURRENT BUILD — BASELINE RECONCILIATION (2026-08-31)
- Uses the user-supplied H10F4 Win11 build as a behavior reference, not a wholesale rollback target; reviewed Win10 H22-H34 fail-closed paths remain installed.
- H11 emergency rendering now preserves resident/fading subtitle rows with cheap plain glyphs instead of silently collapsing a multi-subtitle preset to the current row only.
- NetEase native Seek is held for a second coherent sample before publishing a formal discontinuity; one-frame detector jumps no longer move presentation immediately.
- QQ instrumental first attach can complete duration only after current track identity + lyric candidate duration + two matching QQ UIA current/total samples agree.
- KuGou QQ/NetEase -> already-playing attach now enforces synthetic-zero rejection at the local-master seed function, closing manual/lazy zero bypasses.
- Win10 defaults the existing playback-scoped cyclic-GC guard on (env opt-out retained); Win11 keeps the prior default.
- Equivalent same-track artist separators can complete duration without media reset; subtitle-capacity changes now log their caller chain.
- Canonical release suite: 95 gates.

H34 CURRENT BUILD — KUGOU AUTO OVERLAY + LATE ATTACH + VISUAL DRIFT + COM GUARD (2026-08-31)
- KuGou auto-track lyrics now preserve the running-session overlay intent across every track switch; a successful new payload is verified visible/running and no longer requires another manual Start click.
- QQ/NetEase -> already-playing KuGou is treated as a late attach, not a fresh song-zero start; existing-playback attach waits for real clock/visual/user-seek evidence.
- KuGou H30 visual fallback is cross-Windows but large corrections now require independent same-geometry temporal re-proof; field-style 12s -> 190s/153s/64s false rails are rejected and their lease is revoked.
- Win10 UIA/MSAA fail-closed rules remain unchanged; Win11 UIA/GSMTC remain stronger auxiliary evidence.
- Cyclic GC is deferred out of Qt nativeEvent so comtypes Release/__del__ cannot run in the input-synchronous callback that produced Windows exception 0x8001010d.
- Canonical release suite: 94 gates.

H33F2 RELEASE GATE SPAWN HARDENING (2026-08-30)
- Fixes Windows aggregate-suite exit=125 / FileNotFoundError storms after some gates have already passed.
- Release gates now prefer sys._base_executable (stable base Python) instead of repeatedly launching the SUBST-drive build-venv python.exe; all canonical gates are stdlib-only.
- Windows gate concurrency defaults to 2 unless LIMBUSLYRIC_GATE_WORKERS is explicitly overridden.
- Popen FileNotFoundError gets bounded retries and diagnostics that distinguish a truly missing python/script/cwd from a transient CreateProcess failure.
- Keeps H33F1 H22 next-call fuse hardening and all H31-H33 runtime behavior unchanged.
- Canonical release suite: 93 gates.

H33F1 H22 FUSE NEXT-CALL HARDENING (2026-08-30)
- Fixes the full concurrent release-suite H22 regression: after a slow/no-range KuGou HostV2 provider call arms a fuse, the next logical re-entry is now suppressed at least once even if Windows/VM scheduling advances wall-clock beyond the fuse deadline before the process runs again.
- A changed KuGou HWND clears the one-shot barrier and still receives one fresh capability attempt.
- No H22 gate relaxation: the historical H22 replay is unchanged; H33F1 adds a deterministic scheduler-jump replay.
- Canonical release suite: 92 gates.

H33 QQ CAPACITY + PLAYER EPOCH CLOSURE (2026-08-30)
- QQ capacity residency now also survives direct landing inside provider-confirmed idle gaps; held subtitles stay visible up to max_visible_subtitles.
- Auto transport hints are stamped to selected player + MediaSync player epoch, so QQ/KuGou hints cannot survive a player switch and accelerate another player's track transaction.
- Keeps H32 KuGou hook+poll seek observer and NetEase native position sanity unchanged.
- Canonical release suite: 91 gates.

H32 CURRENT BUILD — KUGOU SEEK OBSERVER + QQ CAPACITY + NATIVE POSITION SANITY (2026-08-30)
- KuGou: cached HostV2/DWM geometry now seeds the seek observer even when the shared visual rail has not locked.
- KuGou: WH_MOUSE_LL remains primary, but a non-injecting GetAsyncKeyState observer runs alongside it so a successfully-installed yet silent hook cannot suppress real user seek observation.
- QQ: renderer residency now uses the same capacity semantics as NetEase; held history survives provider-idle/seek boundaries up to the configured visible-stack limit. QQ GSMTC/UIA clock arbitration is unchanged.
- NetEase native 2.0.6: detector position is normalized against track duration; epoch/wall-clock-like values are rejected before seek authority. Native track-serial edges revoke H31 continuity immediately.
- Field-log residual: one QQ Atlas build-budget fuse / 703ms max frame was observed; existing render fail-safe handled it, so H32 does not alter renderer scheduling.
- Canonical suite: 90 gates including H32.

H31 CURRENT BUILD — REFERENCE NATIVE + TRUSTED CONTINUITY (2026-08-30)
Canonical suite: 89 gates including H31.
Reference transplant: NetEase detector 2.0.6 + 20s async startup + trusted-anchor presentation continuity.
Cold start remains UNKNOWN; seek/track/player epoch revoke continuity; KuGou/QQ authority unchanged.

H30 CURRENT BUILD — SHARED VISUAL TRANSPORT ENGINE / CROSS-WINDOWS CLOSURE (2026-08-30)
- NetEase and KuGou screen-derived progress now use one shared temporal rail/proof/lease/seek state machine on Win10 and Win11.
- Win10 frozen keeps accessibility fail-closed; Win11 GSMTC/UIA/native evidence remains available as auxiliary authority.
- Sparse VM/DWM duplicate frames retain proof identity and a bounded verified-anchor lease instead of immediately dropping to UNKNOWN.
- CloudMusic LyricFetcher/UI reader/Async reader/liveness PID paths share one exact HWND/PID witness and do not call tasklist/EnumProcesses.
- Canonical suite: 88 gates including H30.

H29 CURRENT BUILD — NETEASE TEMPORAL RAIL + CACHED PROCESS WITNESS CLOSURE (2026-08-30)
- Win10 NetEase: H29 directly owns the safe visual clock; temporal pixel motion is the position boundary, static pixels are geometry-only.
- NetEase local rail geometry is inferred around the moving boundary; broad full-row static candidates are fallback-only.
- Verified temporal anchors survive short missed-frame gaps for <=2.6s, then fail closed to UNKNOWN.
- NetEase liveness reuses validated HWND/PID and checks that single PID; positive path does not EnumProcesses/tasklist.
- Win11/non-safe NetEase delegates H28 unchanged. Canonical suite: 87 gates including H29.

H28 CURRENT BUILD — UNIFIED EVIDENCE EPOCH + SEEK OBSERVER CLOSURE (2026-08-30)
- Win10 KuGou: pure Win32/DWM host geometry keeps WH_MOUSE_LL seek observation usable with UIA/MSAA disabled.
- KuGou track changes now retire both transport-clock evidence and LyricWindow precision/display-clock evidence.
- KuGou unknown-duration persistent cache rows are version-unsafe and are purged before use.
- NetEase Win10: PSAPI positive liveness skips slow tasklist; prolonged H26 proof-pending may use H25 temporal physical-rail proof as an independent screen-only fallback.
- H28 runtime ownership is explicitly release-gated; H1-H27 protected methods remain source-locked.

H27: Player clock epoch + Win10 NetEase visual + KuGou Win11 closure; field-evidence regression gate included.
H26 Transport Ownership + Win10 Evidence closure (2026-08-29)
- H25 field replay was not end-to-end: Win10 NetEase could still publish generic auto-local 0ms after native/GSMTC/visual evidence all failed. H26 keeps that state UNKNOWN through snapshot/renderer.
- Selected player now owns desktop transport evidence: cross-provider QQ UI duration, built-in player PID/window probes and cross-player UIA poll requests are rejected; network lyric providers remain usable.
- NetEase native detector start/stop is bounded and exposes starting/timeout/ready lifecycle state instead of remaining indefinitely ambiguous.
- Win10 frozen NetEase visual clock uses static-rail geometry + >=3 samples / >=900ms real forward-motion proof; static lines and stale samples cannot lock.
- Same-track strong player position may correct metadata-detection latency; weak/cross-player evidence preserves the historical fallback.
- Win11/non-safe NetEase delegates the H25 snapshot/auto-local/visual path unchanged. Legacy 481-function lock, KuGou golden lock and QQ protected adapter lock remain required.
- Canonical suite: 84 gates including H26.

See TRANSPORT_OWNERSHIP_WIN10_EVIDENCE_H26_NOTE_20260829.md and CLOCK_SEEK_EVIDENCE_H25_NOTE_20260829.md.

------------------------------------------------------------------------

H25 Clock/Seek/Evidence closure (2026-08-29)
- Full H24 field-log replay: NetEase Win10 safe mode had lyrics/duration but no usable playback position (`position=none`), so normal progress could remain 0.00 and seek had no deterministic re-anchor.
- NetEase Win10 frozen now keeps Chromium UIA/MSAA fail-closed but may use a conservative Win32/GDI screen-pixel rail clock: 3 coherent samples before lock; learned-rail seek is provisional and must be visually reconfirmed; paused seek stays paused; absent proof stays UNKNOWN.
- KuGou provider/cache duration cannot self-certify; manual/auto Host enrichment shares marquee/shell credibility filtering; committed rail seek gets a narrow same-track transport-zero veto.
- Manual KuGou session payload is protected from unsupported same-instance automatic duration downgrade.
- Canonical suite: 83 gates including H25.

See CLOCK_SEEK_EVIDENCE_H25_NOTE_20260829.md and WIN10_RUNTIME_SAFETY_H24_NOTE_20260829.md.

------------------------------------------------------------------------

H24 Win10 runtime safety (2026-08-29)
- NetEase Win10 frozen: Chromium/UIA wake/targeted/deep fallback is fail-closed by default.
- KuGou: rotating Host title cannot own identity; H21 GSMTC veto is cleared during marquee.
- KuGou: session-verified duration blocks unsupported same-title cache downgrade.
- Corrupt auto lyric cache self-heals to valid JSON.

H22 · KuGou Win10 Crash Hardening · 2026-08-28
- Win10 frozen: suppress repeated in-process KuGou deep UIA scans that field logs showed taking ~20–26s and preceding an ungraceful process disappearance.
- Other environments: slow/no-Range HostV2 UIA scans now circuit-break instead of re-entering indefinitely.
- Native/runtime fault stacks are appended to the normal session log through faulthandler when available.
- Dedicated gate: installer/CHECK_KUGOU_WIN10_CRASH_HARDENING_H22_REPLAY.py

KUGOU IDENTITY + COVER UI H21 (2026-08-28)

- Current candidate: H20 classic exit/palette/KuGou packaging baseline + H21 real-machine KuGou identity race closure.
- A fresh Host-V2 switch now vetoes a conflicting stale KuGou GSMTC title-change for 3s while the host title remains current, preventing new-track -> old-track double reset/presentation relaunch.
- KuGou auto-precision may reuse fresh Host-V2 duration during H14's bounded version proof, and a late KRC is rescued only when Host-V2/KRC durations agree narrowly.
- `歌词颜色跟随封面` is relocated beside per-song random font/random color without changing its config key or mutual exclusion semantics.
- Player timing/seek/provider timestamp authority is unchanged. Canonical suite: 79 gates including H21.

See KUGOU_IDENTITY_COVER_UI_H21_NOTE_20260828.md and CODEX_FINAL_HANDOFF.md.

------------------------------------------------------------------------

COVER FOLLOW + MIRRORED EXIT H19 (2026-08-28)

- Current candidate: H18 liveness corroboration + H19 presentation closure.
- Cover-follow now enriches old NetEase search rows that contain picId but no picUrl, keeps H17 per-track serial isolation, uses bounded background requests, and backs failed retries off to 120s.
- Cover color selection uses a dominant hue cluster instead of averaging unrelated cover colors.
- Exit effects now include a recommended per-line reverse of the actual entrance animation: soft/typewriter/rise/slide/bounce, while explicit whole-line fade and instant remain available.
- H19 mirrored exits have a 620-1200ms wall-clock lifetime and remove the legacy generic upward drift.
- Player timing/seek/provider authority is unchanged. Canonical suite: 77 gates including H19.

See COVER_EXIT_MIRROR_H19_NOTE_20260828.md, LIVENESS_CORROBORATION_H18_NOTE_20260828.md and CODEX_FINAL_HANDOFF.md.

------------------------------------------------------------------------

LIVENESS CORROBORATION H18 (2026-08-28)

- Current candidate: H16 UX runtime wiring + H17 runtime hardening + H18 false-dead liveness corroboration.
- All canonical tasklist subprocess paths now have a hard timeout; old UIA/title PID lookups can no longer wait forever.
- NetEase native Stop→Start creates fresh asyncio loop-owned primitives instead of reusing an Event from a retired worker loop.
- Cover-follow publishes results through a queue with per-track request serials, so fast track switches/stale workers cannot overwrite the current request.
- Player timing/seek/provider authority and H16 presentation semantics are unchanged. Canonical suite: 76 gates including H18.

See LIVENESS_CORROBORATION_H18_NOTE_20260828.md, RUNTIME_HARDENING_H17_NOTE_20260828.md and CODEX_FINAL_HANDOFF.md.

------------------------------------------------------------------------

UX RUNTIME WIRING H16 (2026-08-28)

- Current candidate: H15 R4 frontend/updater baseline + H16 real-machine UX runtime wiring closure.
- Font size is direct 6–300 pt keyboard input and restores saved values above the old 100pt cap.
- Exit effects have a perceptible wall-clock lifecycle; only explicit “直接消失” is instant.
- Cover-follow now supports provider NetEase song id / multi-artist matching, retry, normal style-resolver ownership, and mutual exclusion with random color.
- Continuous 1–99 font weight survives lyric restart unless a per-song DIY font_bold rule overrides it.
- “桌面同时字幕” is clarified as “清晰保留字幕”; fading exits remain a separate transition lane.
- Player timing/seek/provider authority is unchanged. Canonical suite: 74 gates including H16.

See UX_RUNTIME_WIRING_H16_NOTE_20260828.md and CODEX_FINAL_HANDOFF.md.

------------------------------------------------------------------------

FRONTEND POLISH R4 + VERIFIED INSTALLER UPDATE H15 (2026-08-27)

- Current candidate: user-supplied R4 control-panel polish on the frozen H14 behavior baseline, plus H15 verified installer handoff.
- R4 changes only presentation/UI; H15 integrates runtime-created H12/H13 cards with R4 spacing/UI-scale baselines.
- Update checking derives the current H/F stage from the build tag instead of the old hard-coded H12 label.
- Newer GitHub/custom releases may auto-download only an HTTPS .exe with SHA-256. Missing digest falls back to opening the HTTPS release page.
- Verified installers download to the per-user update cache via .part + size/hash checks, then the existing installer is launched and LimbusLyric uses its bounded exit path.
- Player timing/seek/provider authority remains H14/H13/H11 behavior.
- Canonical release suite is 73 gates including H15.

See FRONTEND_VERIFIED_UPDATER_H15_NOTE_20260827.md and CODEX_FINAL_HANDOFF.md.

------------------------------------------------------------------------

AUTO PRECISION DEADLINE H14 (2026-08-26)

- Current candidate: H13/H12/H11 baseline + H14 automatic KuGou precise-enhancement lifetime closure.
- Real H10F4 log evidence showed ordinary LRC at ~2.2s while a stale KuGou precise/KRC enhancement survived ~204.8s.
- H14 gives auto KuGou precise enhancement a 22s cooperative budget and max 2 in-flight tasks; the suspicious MSAA version-duration probe is separately bounded to 1.25s / one in-flight probe.
- Missing/timed-out version evidence means keep the fast ordinary lyric; do not accept an unproven same-title KRC result.
- KuGou transport/seek authority, QQ Software-2, NetEase native timing, H11-H13 behavior and manual-fetch semantics are unchanged.
- Canonical release suite is 72 gates including H14.

See AUTO_PRECISION_DEADLINE_H14_NOTE_20260826.md and CODEX_FINAL_HANDOFF.md.

------------------------------------------------------------------------

OUTPUT / DIY / SPOTIFY H13 (2026-08-26)

- Current candidate: H12 UX + H11 responsiveness + H13 output/lyric-slot/Spotify outer extensions.
- Adds exact per-song precise/classic/translation lyric payload slots, a presentation-only OBS mirror window, and a strict Windows-local-GSMTC Spotify player lane.
- Spotify uses no Web API/OAuth and never uses Spotify shell/window title as canonical track identity.
- Automatic in-place EXE replacement remains deferred; H12 HTTPS update check/open remains unchanged.
- Canonical release suite is 71 gates including H13.

See OUTPUT_DIY_SPOTIFY_H13_NOTE_20260826.md, UX_EXTENSIONS_H12_NOTE_20260826.md, POST_RELEASE_RESPONSIVENESS_H11_NOTE_20260825.md and CODEX_FINAL_HANDOFF.md.

------------------------------------------------------------------------

UX EXTENSIONS H12 (2026-08-26)

- Presentation/control-panel extension on top of H11; QQ/NetEase/KuGou timing authority is unchanged.
- Adds UI scaling, lyric opacity, continuous font weight, micro-jolt shake, cover-follow color, six exit effects, safe capture exclusion, and HTTPS update checking.
- Dedicated OBS mirror output, Spotify, three-slot per-song lyric payloads, and in-place self-updater are intentionally deferred.
- Canonical release suite is 70 gates including H12.

See UX_EXTENSIONS_H12_NOTE_20260826.md, POST_RELEASE_RESPONSIVENESS_H11_NOTE_20260825.md and CODEX_FINAL_HANDOFF.md.

------------------------------------------------------------------------

POST-RELEASE RESPONSIVENESS H11 (2026-08-25)

- Current candidate closes the post-release issues reproduced from the 2026-08-22..25 raw-log bundle without replacing the locked QQ/KuGou/NetEase time authorities.
- QQ gesture and NetEase page/hover control operations are fire-and-forget from GUI snapshot callers; slow/hung helpers are isolated on daemon workers with CALL watchdog telemetry.
- Manual lyric fetch is an asynchronous transaction. Precise mode may publish a fast ordinary lyric first, then upgrade in background; stale/late results are generation-rejected and the GUI has a bounded wait.
- Player liveness keeps H10 multi-sample death proof but isolates a hung Toolhelp snapshot. A timed-out native observation falls back to bounded tasklist; failed/blank tasklist output is UNKNOWN, never false-dead, and late Toolhelp results are stale-dropped.
- QQ status=unknown may cold-start only from a strict duration-matched validated advancing UIA stream; candidate/static/hover-like samples remain non-authoritative.
- Confirmed new-track presentation ownership prevents an older lyric payload from reviving after a failed fetch. trans_only remains a user preference when the current song has no translation.
- Suspicious KuGou HostV2 titles cannot override more credible identity; repeated HostV2 misses back off.
- Atlas inflight/fuse or a proven expensive paint switches presentation to a cheap glyph fallback; lyric timing is unchanged.
- Application exit is bounded and instrumented so a stuck provider/helper cannot leave the UI waiting indefinitely.
- Canonical release suite is 69 gates and now includes H11 plus the previously omitted V14/V16/V17 replays.

See POST_RELEASE_RESPONSIVENESS_H11_NOTE_20260825.md and CODEX_FINAL_HANDOFF.md before changing this candidate. Historical notes below remain useful provenance.

------------------------------------------------------------------------

RELEASE-REVIEW CLOSURE H10F2 (2026-08-20)

- QQ startup-attach no longer trusts one transient validated Text sample for display-only seeding; it waits for the existing advancing-stream proof. Auto-track fast display seed and formal QQ clock/Seek authority are unchanged.
- KuGou WH_MOUSE_LL stays installed for reliability, but inactive/dead KuGou no longer queues global mouse edges or forces per-frame queue replacement on other players.
- H10/H10F1 player-death and dead-player fetch guards remain unchanged.

PLAYER-LIVENESS CLOSURE H10 (2026-08-20)

- Fixes confirmed ghost playback: when a selected built-in player has really exited, old lyrics/local clocks cannot keep advancing.
- QQ / NetEase / KuGou share one process-liveness authority. Three consecutive background process snapshots are required before death is confirmed; GSMTC loss or hwnd=0 alone is never enough.
- Process liveness is checked on a daemon worker with Toolhelp32, outside Qt/render/snapshot. Slow security-software hooks therefore delay only exit confirmation, not lyric rendering.
- Confirmed death retires H9 provisional display clock, QQ loop clock, KuGou rail-local clock, UIA/Seek anchors and provider primary clocks; public state becomes stopped with no position.
- LimbusLyric stays armed-idle after the player exits. Reopening the same player restores cached lyrics only after a real player position exists; another song follows the normal auto-track transaction.
- H9 QQ/NetEase UNKNOWN-status provisional transport optimization is retained. QQ's conservative ~1.8s lyric-version verification window is still unchanged.

AUTO-TRACK PROVISIONAL TRANSPORT H9 (2026-08-19)
- Different-song QQ/NetEase display-only clock may carry a fresh pre-switch PLAYING state through transient UNKNOWN; explicit PAUSED/STOPPED veto it.
- KuGou keeps H7 rail-local transport/pause authority unchanged.

GUI HOTPATH RELEASE CLOSURE H8F2 FINAL (2026-08-19)
- Slow cross-process target discovery is isolated from GUI hot paths.
- Fault injection remains release-test-only and is excluded from runtime packaging.

H53: fixes literal 0% center avoidance, differentiates staggered per-char exit from directional wipes, and adds opt-in matrix-only 3D depth layers.

H78 STUDIO NAVIGATION + TYPOGRAPHIC LAYOUT (2026-09-05)
- Right-title utility is permanently “界面”; H76 background-state updates can no longer rename it to “背景”.
- H51 typography/layout controls move from Settings to Appearance as the real owner card: per-line size range, language fonts, center frequency and conditional multi-monitor target.
- Page transition becomes a visible 245 ms opacity reveal; workspace header settles in 220 ms.
- Top navigation gets a 225 ms moving selection surface plus a quiet editorial workspace index/mark.
- Settings page no longer advertises interface/visual layout controls.
- No transport/provider/seek/lyric lifecycle/config-schema ownership changes.
- Dedicated gate: CHECK_STUDIO_NAVIGATION_TYPOGRAPHIC_LAYOUT_H78_REPLAY.py
