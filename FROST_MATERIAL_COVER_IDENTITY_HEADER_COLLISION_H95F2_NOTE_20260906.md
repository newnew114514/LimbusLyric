# H95F2 · Frost Material / Cover Identity / Header Collision

- 背景“磨砂”不再等同于单纯放大模糊半径：保留少量原图结构，叠加轻雾化与缓存微颗粒材质；0% 仍为原图。
- 共享唱片封面改为按播放器完整歌手署名做授权。搜索别名/标题括号仅用于召回，不再因为一个共同演唱者就授权另一套主创署名/发行版本。
- 封面 JPEG 缓存升级到 H95F2 身份 schema；旧封面/跟色证据不会静默沿用。
- `LISTENING ROOM` 改为实际检查同步状态胶囊几何位置，有碰撞或安全间距不足时不绘制；紧凑 Hero 下同步状态文案自动缩短。
- 不修改播放器时钟、Seek、歌词时序、H95 同步医生或歌词内容选择权威。
