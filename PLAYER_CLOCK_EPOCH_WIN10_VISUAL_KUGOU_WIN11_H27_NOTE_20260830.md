# H27 Player Clock Epoch + Win10 Visual + KuGou Win11 Closure

H27 closes two field-confirmed clock failures without changing protected historical methods.

- Win10 NetEase frozen safe path keeps UIA/Chromium disabled. Zero-touch window discovery now uses EnumWindows + process-image verification, and visual rail discovery requires a long two-colour run with one dominant boundary before motion proof.
- KuGou on Windows 11 treats a confirmed new track as a hard clock-evidence epoch. Previous-track local rail/gesture/UIA proof cannot seed the new track.
- KuGou HostV2 proof-pending on Windows 11 no longer counts as H22 no-range misses; H22 Win10 frozen safety remains unchanged.
- Fresh KuGou new-track transport evidence blocks stale Host-title rollback from restoring cached old lyrics during the short host-lag window.
- H27 Win11 KuGou correctness applies to both source runs and frozen builds (Windows build >= 22000).
