# H46 NetEase transport self-heal

H45 revoked bad native position authority, but a rejected snapshot could still publish `paused`
and freeze shared state while process-affine GSMTC advanced. H46 removes both position and transport
authority from rejected native samples. Trusted CloudMusic GSMTC directly repairs the stale public,
UIA, and estimator anchors on the affected Win10 path (or during a short rejection quarantine).
Starting while a confirmed new-track lyric request is in flight also keeps automatic reading armed.

Regression: `installer/CHECK_NETEASE_TRANSPORT_SELF_HEAL_H46_REPLAY.py`.

