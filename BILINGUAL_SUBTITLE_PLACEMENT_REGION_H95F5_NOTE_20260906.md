# H95F5 · 双语字幕 + 字幕活动区域

- 双语模式不会替换原歌词时间轴。原歌词继续使用现有 QRC/KRC/YRC/普通 LRC 与 H95 最终显示时钟；翻译仅作为行级 sidecar 对齐到 H95 Unified Lyric Track。
- 双语开启后后台按当前歌曲身份、来源与可用时长调用现有“三平台仅翻译”安全搜索；同一歌曲的并发请求会合并。没有安全翻译时自动保持单语。
- 翻译和原文共用一个出生锚点。翻译字号默认为主歌词 74%，并保留独立行间距设置；当前行与之后生成的历史/退场行都携带翻译 sidecar。异步翻译晚到时只做最小范围校正，不重新随机当前行。
- 字幕活动区域支持：全屏、仅上半屏、仅下半屏、自定义百分比矩形，以及 0–15% 屏幕相对安全边距。百分比按当前字幕显示器实时换算，避免分辨率变化导致绝对像素失效。
- 区域约束不替换 H61 成熟碰撞布局：每条歌词只调用一次现有 H61 placement，保留其角度、景深、随机样式与碰撞选择，然后只在请求区域内重新选择最终锚点。这样避免为了找区域候选重复执行 H61 造成 CPU 与日志突发。
- 如果字幕组本身比用户指定区域还大，会居中到该维度而不是裁字；这是“文字完整可读”优先于硬裁剪的明确降级。
- 同标题/歌手的翻译缓存还会核对可用时长；明显不同版本不会直接复用旧 sidecar。
- H95F5 只拥有显示 sidecar、出生范围与设置持久化；不接管 MediaSessionSync、Seek、LyricSearchEngine 主搜索、parse_lrc 或播放器身份。
