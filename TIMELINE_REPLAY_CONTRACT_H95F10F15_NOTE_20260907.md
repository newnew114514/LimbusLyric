# H95F10F15 Timeline Replay Contract

Adds a common fixture schema for timeline races and an opt-in, state-change-only RenderPlan trace ring. Release replay executes existing mature F5/F6/F8 helpers directly instead of inventing a second player/provider state machine. Runtime trace is disabled by default and stores compact resolved state only.
